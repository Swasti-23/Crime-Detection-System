<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        :root {
            --primary: #092635;
            --secondary: #1B4242;
            --gradient1: #5C8374;
            --gradient2: #9EC8B9;
            --gradient3: #cad36a; /* Start color of gradient */
            --gradient4: #9eaac8;
        }
        body {
            background-color: var(--gradient2); /* Example color */
        }
        #mainav{
            background-color: var(--gradient1);
        }
        .emergency-box {
            background-image: linear-gradient(to right, var(--gradient3), var(--gradient4)); 
            border: 1px solid rgba(255, 255, 255, 0.2); 
            padding: 10px; 
            margin-bottom: 20px;
            border-radius: 8px; 
            margin-left: 40%;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); 
            max-width: 300px;
            position: relative;
        }

        .emergency-box .name {
            font-size: 18px; 
            font-weight: bold; 
            color: black; 
        }

        .emergency-box .age,
        .emergency-box .phone {
            font-size: 16px; 
            color: black; 
        }

        .delete-button {
            position: absolute;
            top: 5px;
            right: 5px;
            background-color: transparent;
            border: none;
            color: #fff;
            cursor: pointer;
            padding: 0;
            font-size: 14px;
            transition: color 0.3s;
        }

        .delete-button:hover {
            color: red;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg bg-body-tertiary" data-bs-theme="dark" id="mainav">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Home</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="#">Home</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="#">Features</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="#">Pricing</a>
              </li>
              <li class="nav-item">
                <a class="nav-link disabled" aria-disabled="true">Disabled</a>
              </li>
            </ul>
          </div>
        </div>
    </nav>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js" integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js" integrity="sha384-0pUGZvbkm6XF6gxjEnlmuGrJXVbNuzT9qBBavbLwCsOGabYfZo0T0to5eqruptLy" crossorigin="anonymous"></script>
    <h1>Emergencies</h1>
      <% emergencies.forEach((e, index) => { %>

        <div class="emergency-box">
          <a href="/<%=e._id%>"><span class="name">IIIT Naya Raipur</span></a>
        </div>
      <% }); %>

    
</body>
</html>
