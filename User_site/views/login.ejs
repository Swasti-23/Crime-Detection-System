<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        /* CSS to style button */
        :root {
            --primary: #092635;
            --secondary: #1B4242;
            --gradient1: #5C8374;
            --gradient2: #9EC8B9;
        }
        body {
            background-color: var(--gradient2);
        }
        #mainav{
            background-color: var(--gradient1);
        }
        #Emer {
            display: block;
            margin: 0 auto;
            margin-top: 10%;
            border: none;
            outline: none;
            background: none;
            padding: 20px;
            cursor: pointer;
            transition: transform 0.2s;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg bg-body-tertiary" data-bs-theme="dark" id="mainav">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Home</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <a class="navbar-brand" href="/login">Information</a>
            </ul>
          </div>
        </div>
    </nav>
    <div class="row">
        <h1 class="text-center">Information!!</h1>
        <div class="col-6 offset-3">
            <form action="/info" method="POST" novalidate class="needs-validation">
                <div class="mb-3">
                    <label class="form-label" for="name">Name: </label>
                    <input class="form-control" type="text" name="emergency[name]" id="title" required placeholder="Name">
                    <div class="valid-feedback">looks good!!</div>
                </div>
                <div class="mb-3">
                    <label class="form-label" for="age">Age: </label>
                    <input class="form-control" type="number" name="emergency[age]" id="location" required placeholder="Location">
                    <div class="valid-feedback">looks good!!</div>
                </div>
                <div class="mb-3">
                    <label for="price" class="phone">Phone: </label>
                    <div class="input-group">
                        <span class="input-group-text" id="price-label">+91</span>
                        <input type="text" class="form-control" placeholder="0.00" id="price" required aria-label="price" aria-describedby="price-label" name="emergency[phone]">
                        <div class="valid-feedback">looks good!!</div>
                    </div>
                </div>
                <div class="mb-3">
                    <button class="btn btn-success" type="submit">Submit</button>
                </div>
            </form>
            <a href="/">Cancel</a>
        </div>
    </div>
</body>
</html>
